name: Python package

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.9', '3.8', '3.5-dev', '3.6-dev', 'nightly']

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        pip install -r test_requirements.txt
        pip install .
        
        pip install .[test]
        pip install 'coverage>=4.0,<4.4' codeclimate-test-reporter
    - name: Run tests
      run: |
        py.test --cov=pycec tests/
        coverage run --source=pycec setup.py test
        coverage run --source=pycec `which py.test`
    - name: Upload coverage to Code Climate
      env:
        CC_TEST_REPORTER_ID: ${{ secrets.CC_TEST_REPORTER_ID }}
      run: codeclimate-test-reporter
    - name: Upload coverage to Coveralls
      env:
        COVERALLS_REPO_TOKEN: ${{ secrets.COVERALLS_REPO_TOKEN }}
      run: coveralls