[tox]
envlist = py39, black

[testenv]
deps =
    requirements.txt
commands =
    coverage run {toxinidir}/manage.py test

[testenv:black]
deps =
    requirements.txt
    black
commands = black -t py39 -l 120 --exclude 'migrations' stregsystem stregreport kiosk
